{
  "name": "Naruto",
  "version": "1.0.0",
  "scripts": {
    "front": "cd front && npm start",
    "back": "cd back && node server.js",
    "heroku-postbuild": "cd front && npm install && cd back && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parse": "^5.1.0",
    "express": "^4.18.1"
  }
}
